dist: trusty
sudo: required

language: python
python: "3.6"

before_install:
    - sudo pip install --upgrade cython
    - sudo pip install --upgrade kivy
    - sudo pip install --upgrade kivymd
    - sudo pip install --upgrade buildozer


script:
    - buildozer android debug
